/**
 * Schema barrel — passed to both the drizzle instance and the better-auth
 * adapter. Add your own table modules here as you build domains:
 *
 *   export * from "./post"
 */
export * from "./auth"
